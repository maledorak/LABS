---

- name: Install network packages
  pacman:
    state: present
    name:
      - networkmanager
      - network-manager-applet
      - openconnect
      - networkmanager-openconnect
      - gnome-keyring
      - seahorse
  tags:
    - network
    - install_pacman
